import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'template_blank',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
